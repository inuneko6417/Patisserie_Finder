<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ユーザー登録</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <!-- SwiperのCSSとJS -->
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" %>
  </head>
  <body>
    <% unless current_page?(root_path) %>
      <% if user_signed_in? %>
        <%= render 'shared/header' %>
      <% else %>
        <%= render 'shared/header_login' %>
      <% end %>
    <% end %>

    <!-- Flash メッセージ -->
    <% if flash[:success] %>
      <div class="p-4 mb-4 text-green-800 bg-green-200 rounded-lg">
        <%= flash[:success] %>
      </div>
    <% elsif flash[:error] %>
      <div class="p-4 mb-4 text-red-800 bg-red-200 rounded-lg">
        <%= flash[:error] %>
      </div>
    <% end %>

    <%= yield %>
    <footer class="bg-gray-100 border-t mt-12 py-4 text-center text-sm text-gray-500">
      <div class="container mx-auto">
        &copy; <%= Time.current.year %> MyApp |
        <%= link_to '利用規約', terms_path, class: "underline hover:text-blue-500" %> |
        <%= link_to 'プライバシーポリシー', privacy_path, class: "underline hover:text-blue-500" %>
      </div>
    </footer>
  </body>
</html>
