<div class="relative h-screen">
  <!-- 背景画像 -->
  <div class="absolute inset-0">
    <%= image_tag 'top.jpg', class: "w-full h-full object-cover", alt: "Top Background" %>
  </div>

  <!-- オーバーレイ -->
  <div class="absolute inset-0 bg-black bg-opacity-50"></div>

  <!-- コンテンツ -->
  <div class="relative z-10 flex flex-col items-center justify-center h-full text-center text-white">
    <h1 class="text-4xl md:text-6xl font-bold mb-6">ようこそ、Patisserie Finderへ</h1>
    <p class="text-lg md:text-xl mb-8">
      あなたの街で一番おいしいスイーツを探しましょう。
    </p>
    <div class="flex space-x-4">
      <%= link_to 'スイーツを探す', posts_path, class: "px-6 py-3 bg-yellow-500 hover:bg-yellow-600 rounded-lg text-lg font-semibold transition duration-300" %>
      <%= link_to 'お問い合わせ', '#', class: "px-6 py-3 bg-gray-700 hover:bg-gray-800 rounded-lg text-lg font-semibold transition duration-300" %>
    </div>
  </div>
</div>
